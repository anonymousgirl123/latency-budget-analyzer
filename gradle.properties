pluginGroup=com.kamini
pluginVersion=1.0.0

platformVersion=2023.3
platformType=IC
pluginSinceBuild=233

# ── Marketplace publish token ──────────────────────────────────────────────
# DO NOT add your token here — this file is committed to source control.
# Instead, add the following line to ~/.gradle/gradle.properties (user-level):
#
#   intellijPublishToken=YOUR_TOKEN_HERE
#
# Alternatively, export it as an environment variable before publishing:
#   export INTELLIJ_PUBLISH_TOKEN=YOUR_TOKEN_HERE
#   ./gradlew publishPlugin